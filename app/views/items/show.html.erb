


<section class="sets">
  <div class="sets col-xs-12 col-sm-6 col-md-12">
   <div class="col-xs-12 col-sm-12 col-md-4">
    <h1><%= @item.title %></h1>
    <%= image_tag (@item.avatar.large.url) %>
    </div>
  </div>
  <div class="sets col-xs-12 col-sm-6 col-md-12">
   <div class="col-xs-12 col-sm-12 col-md-4">
    <h3><%= @item.description %></h3>
  </div>
  </div>
  <div class="sets col-xs-12 col-sm-6 col-md-12">
  <ul class="meta-search">
    <div class="col-xs-12 col-sm-12 col-md-2">
        <li><i class="fa fa-money fa-3 large_fav"></i><span style="font-size: 20px"> MYR<%= @item.price_per_day %></span></li>
    </div>
    <div class="col-xs-12 col-sm-12 col-md-2">
      <li><i class="fa fa-location-arrow fa-3 large_fav"></i><span style="font-size: 20px"><%= @item.user.locality %></span></li>
    </div>
    <div class="col-xs-12 col-sm-12 col-md-2">
      <li><i class="fa fa-tag fa-3 large_fav"></i> <span style="font-size: 20px"><%= @item.tag_list.first %></span></li>
    </div>
    </ul>
  </div>
</section>
<br>
<br>
<hr>
<br>
<span>
 <a href="/users/<%= @item.user_id %>">
  <%= image_tag ((User.find(@item.user_id)).avatar.medium.url), :class => "img-circle" %>
</a>
</span>

<h2><%= @item.user.first_name %></h2>
<% if @item.user.avatar.present? %>
<span>
  <img src="<%= @user.avatar.thumb.url %>" class="img-circle" alt="">
</span>
<% else %>
<img class="avatar" src="http://placehold.it/64x64">
<% end %>


<div class="row">
  <div class="spacer"></div>
  <div class="col-md-12">
    <%= link_to "Ask a question about this item", new_conversation_path(item_id: @item.id), class: "btn btn-success" %></div>
    <div class="spacer"></div>
  </div>

  <%= image_tag (@item.avatar.medium.url) %>
  <p><%= link_to "RENT THIS", new_item_rental_path(@item.id), method: :get, :class => "button success" %></p>

  <div>

  <!--   < % if @item.avatars.present? %>
  < % @item.avatars.each do |a| %> -->
  <!--       <img src="< %= a.large.url %>" alt=""> -->
  <%= link_to "Edit", edit_item_path(@item.id), method: :get %>
  <%= link_to "Delete", item_path(@item.id), method: :delete %>
</div>

</div>